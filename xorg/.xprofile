#! /bin/sh

if command -v xinput >/dev/null 2>&1; then
    if xinput list --name-only 2>/dev/null | grep -Fxq "Surface Touch Device"; then
        xinput disable "Surface Touch Device"
    fi
fi

(sleep 10 && xmodmap "$HOME/.config/xmodmap") &
